import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.citruscodex.carnetagrumes',
  appName: 'Carnet Agrumes',
  webDir: '.',
  bundledWebRuntime: false
};

export default config;
